# Quality of Life Syntax Sugar

Par gives you a set of very light core constructs. Everything else is built on
those foundations. Some bits of syntax, though, exist purely to make common
patterns less noisy.